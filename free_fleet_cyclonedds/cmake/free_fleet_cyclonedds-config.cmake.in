@PACKAGE_INIT@

get_filename_component(free_fleet_cyclonedds_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

include(CMakeFindDependencyMacro)

find_dependency(rmf_utils REQUIRED)
find_dependency(rmf_traffic REQUIRED)
find_dependency(free_fleet REQUIRED)
find_dependency(CycloneDDS REQUIRED)

if(NOT TARGET free_fleet_cyclonedds::free_fleet_cyclonedds)
    include("${free_fleet_cyclonedds_CMAKE_DIR}/free_fleet_cyclonedds-targets.cmake")
endif()

check_required_components(free_fleet_cyclonedds)
